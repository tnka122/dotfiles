name: macOS

on:
  push:
    paths:
      - 'Makefile'
      - 'scripts/*'
      - 'brew/*'

jobs:
  MacOS-Setup:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3
      - run: make all
      - name: Check commands
        run: zsh .github/scripts/check-commands.sh
